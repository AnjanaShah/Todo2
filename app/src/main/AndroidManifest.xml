<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".meetingActivity" />
        <activity android:name=".MeetingListActivity" />
        <activity android:name=".MeetingAddActivity" />
        <activity android:name=".MeetingViewActivity" />
        <activity android:name=".NotesAddActivity" />
        <activity android:name=".notesActivity" />
        <activity android:name=".NotesViewActivity" />
        <activity android:name=".NotesListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExpenseListActivity" />
        <activity android:name=".ExpenseAddActivity" />
        <activity android:name=".ExpenseViewActivity" />
        <activity android:name=".ContactAddActivity" />
        <activity android:name=".ContactListActivity" />
        <activity android:name=".ContactViewActivity"></activity>
    </application>

</manifest>